import java.util.ArrayList;

public class GFG {

	public static void main(String[] args) {
		/*
		 * String[] arr = { "3", "30", "34", "5", "9" }; Arrays.sort(arr, (a, b) -> {
		 * String o1 = a + b; String o2 = b + a; return o2.compareTo(o1); });
		 */
		/* StringBuilder sb = new StringBuilder(); */
		/*
		 * for (String s : arr) sb.append(s); String s = sb.toString();
		 * System.out.println(s);
		 */
		ArrayList<Integer> A = new ArrayList<Integer>();
		A.add(3);
		
		
	}
}
